<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.bt.signinandout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/calender"
        android:supportsRtl="true"
        android:theme="@style/Theme.Signinandout">
        <activity android:name=".Calculator"></activity>
        <activity android:name=".expenseCategory" />
        <activity android:name=".Login" />
        <activity android:name=".register" />
        <activity android:name=".MainActivity"
            android:theme="@style/Signinandout.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>